{
  "name": "openvide-bridge",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx tsx http-bridge.ts",
    "start:notls": "npx tsx http-bridge.ts --no-tls",
    "build": "npx tsc http-bridge.ts certs.ts --outDir dist --module nodenext --moduleResolution nodenext --target es2022"
  }
}
